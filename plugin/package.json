{
  "name": "@prism3/plugin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Prism3 Figma plugin (docs/18, docs/22). Two contexts: a main-thread controller that writes figma.variables, and the shared web UI in the iframe. #107 is the scaffold — manifest + two-context split + typed postMessage bridge (vanilla); the write adapter (#108) and the shared-UI bundle (#110) land later.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit -p tsconfig.main.json && tsc --noEmit -p tsconfig.ui.json"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.131.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
